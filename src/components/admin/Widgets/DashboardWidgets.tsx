import React from 'react';

type Props = {
  open?: boolean;
  value?: number;
};

const DashboardWidgets: React.FC<Props> = ( props:Props) => {
  return (
    <div>
      
    </div>
  );
};

export default DashboardWidgets;
